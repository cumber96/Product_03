# DEVELOPMENT.md — 개발 환경 및 진행 절차

> 이 문서는 Xcode 프로젝트가 생성되기 전 시점에 작성되었습니다. 실제 Xcode 프로젝트가
> 생성되면 빌드/실행 방법 등 구체적인 절차를 이 문서에 추가해야 합니다.

## 1. 현재 상태

이 저장소에는 아직 Xcode 프로젝트와 애플리케이션 코드가 없습니다. 현재는 문서와 폴더 구조만
존재합니다.

## 2. 예상 개발 환경 (가정)

watchOS 앱과 iOS 앱을 함께 다루는 프로젝트라는 점에서, 일반적으로 아래와 같은 환경이 필요할
것으로 예상됩니다. 정확한 버전 요구사항은 실제 Xcode 프로젝트 생성 시점에 확정합니다.

- macOS + Xcode (버전 미정 — 사용할 watchOS/iOS SDK에 따라 결정 필요)
- Apple Developer 계정 (HealthKit, 워치 앱 등은 실기기 테스트 시 필요할 가능성이 높음)
- 테스트용 Apple Watch 및 페어링된 iPhone 실기기
  - HealthKit의 심박수 등 센서 데이터, 실제 운동 세션 동작은 시뮬레이터만으로 충분히
    검증되지 않을 가능성이 있습니다. 실기기 필요 여부는 TEST_PLAN.md에서 별도로 다룹니다.

## 3. 프로젝트 생성 전 확인이 필요한 사항

- Xcode 프로젝트를 언제, 어떤 구조로 생성할지 (ARCHITECTURE.md의 "앱 구성 방식" 참고)
- Bundle ID, 개발팀(Team) 설정 등 Apple Developer 계정과 관련된 사전 준비
- HealthKit 사용을 위한 Capability 설정 및 권한 관련 Info.plist 항목 (구체적인 키 값은
  구현 시점에 확정)

## 4. 폴더 구조

현재 저장소에는 향후 Xcode 프로젝트를 담을 자리표시자 폴더만 존재합니다.

```
App/        iOS(iPhone) 앱 타겟 예정 위치 (비어 있음)
WatchApp/   watchOS 앱 타겟 예정 위치 (비어 있음)
Shared/     공용 코드 예정 위치 (비어 있음)
```

Xcode 프로젝트 생성 시 위 폴더 구조를 그대로 사용할지, 아니면 Xcode가 기본으로 생성하는 구조를
따를지는 아직 결정되지 않았습니다.

## 5. 다음 단계 (제안, 확정 아님)

아래는 현재 시점에서 예상되는 다음 단계이며, 실제 진행 순서는 달라질 수 있습니다.

1. Xcode 프로젝트 생성 및 iOS/watchOS 타겟 구성 방식 결정
2. HealthKit 권한 요청 및 실내 러닝 운동 세션 시작 여부에 대한 기술 검증
3. 시간/거리/심박수 측정값을 워치 화면에 표시
4. 일시정지/재개/종료 흐름 구현 및 HealthKit 저장 검증
5. 아이폰 앱에서 실시간 상태 및 기록 조회 기능 검증

이 목록은 TEST_PLAN.md의 검증 항목과 함께 갱신되어야 합니다.
