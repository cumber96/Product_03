# ARCHITECTURE.md — 아키텍처 초안

> 이 문서는 Xcode 프로젝트가 생성되기 전 시점에 작성되었습니다. 실제 구현 시 아래 내용은
> 바뀔 수 있으며, "미정" 또는 "가정"으로 표시된 부분은 확정된 결정이 아닙니다.

## 1. 현재 상태

애플리케이션 코드는 아직 존재하지 않습니다. 이 문서는 1차 목표(README.md, PRODUCT.md 참고)를
달성하기 위해 필요할 것으로 예상되는 구성 요소를 미리 정리한 것입니다.

## 2. 예상 구성 요소 (가정)

실내 러닝 측정 및 기록이라는 목표를 달성하기 위해, 일반적으로 아래와 같은 Apple 프레임워크가
관련될 것으로 예상합니다. 다만 실제 채택 여부와 세부 사용 방식은 기술 검증 과정에서 확인이
필요합니다.

- **watchOS 앱**: 운동 시작/일시정지/재개/종료 UI, 실시간 측정값 표시
- **HealthKit**: 실내 러닝 운동 세션 관리, 심박수 등 건강 데이터 접근, 종료된 운동 저장
  - 실내 달리기 운동 세션 관리에는 `HKWorkoutSession` 계열 API가 관련될 것으로 예상되지만,
    구체적인 API 채택(예: WorkoutKit 사용 여부)은 아직 결정되지 않았습니다.
- **iPhone(iOS) 앱**: 진행 중인 운동 상태 확인, 저장된 러닝 기록 조회
- **워치 ↔ 아이폰 간 데이터 전달**: HealthKit 동기화만으로 충분한지, 별도의 실시간 통신
  (예: WatchConnectivity)이 필요한지는 미정입니다.

## 3. 앱 구성 방식 (미정)

다음 사항은 아직 결정되지 않았으며, Xcode 프로젝트 생성 시점에 확인이 필요합니다.

- 단일 Xcode 프로젝트에 iOS 타겟과 watchOS 타겟을 함께 둘지, 프로젝트를 분리할지
- 워치 앱을 아이폰 앱에 종속된 컴패니언 앱으로 만들지, 독립 실행형(Standalone) watchOS 앱으로
  만들지
- iOS/watchOS 최소 지원 버전
- UI 프레임워크(SwiftUI 가정, 확정 아님)

## 4. 데이터 흐름 (가정, 검증 필요)

아래는 1차 목표를 달성하기 위해 필요할 것으로 보이는 데이터 흐름의 초안입니다. 실제 구현 전
가정 단계이며, 기술 검증을 통해 실현 가능성을 확인해야 합니다.

1. 워치에서 사용자가 실내 달리기를 시작 → 운동 세션 시작
2. 세션 진행 중 시간/거리/심박수 측정 → 워치 화면에 실시간 표시
3. 사용자가 일시정지/재개/종료 조작
4. 종료 시 운동 데이터를 HealthKit에 저장
5. 아이폰 앱이 HealthKit(및/또는 별도 통신 경로)을 통해 진행 상태 또는 저장된 기록을 조회

각 단계에서 실제로 어떤 API와 권한(HealthKit 권한 등)이 필요한지는 구현하며 확인해야 합니다.

## 5. 콘텐츠 엔진과의 관계 (범위 밖)

최종 제품 비전인 "러닝 데이터 → 콘텐츠 진행 변환 엔진"은 이 아키텍처 문서의 범위에 포함되지
않습니다. 현재 단계에서는 이 엔진을 고려한 데이터 구조를 미리 설계하지 않으며, 필요 시 별도
문서에서 다룹니다.

## 6. 폴더 구조 (예정, 자리표시자)

Xcode 프로젝트 생성 전이므로 아래 폴더는 비어 있는 자리표시자입니다.

```
App/        iOS(iPhone) 앱 타겟 예정 위치
WatchApp/   watchOS 앱 타겟 예정 위치
Shared/     공용 코드/모델 예정 위치
```

실제 폴더 구성(타겟 분리 방식, 공용 프레임워크 사용 여부 등)은 미정이며 위 구조와 달라질 수
있습니다.

## 7. 열린 질문

PRODUCT.md의 "열린 질문" 항목과 함께, 아래 사항이 구현 전 확인이 필요합니다.

- HealthKit 실내 러닝 운동 세션이 시뮬레이터에서 검증 가능한지, 실기기가 필수인지
- 심박수 센서 데이터의 신뢰도 및 접근 방식
- 워치 앱이 백그라운드/화면 잠금 상태에서도 측정을 계속할 수 있는지
